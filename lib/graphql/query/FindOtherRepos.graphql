query FindOtherRepos($name: String!, $sha: String!) {
    Repo {
        name
        owner
        branches(name: "master") @required {
            commit {
                analysis(name: $name, sha: $sha) {
                    data
                    sha
                    name
                }
            }
        }
    }
}
