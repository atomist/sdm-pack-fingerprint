query GetAllFpsOnSha($repo: String!, $owner: String!, $branch: String!){
  Repo(name: $repo, owner: $owner) {
        branches(name: $branch) {
            commit {
               pushes {
                fingerprints {
                  name
                  sha
                  data
                }
              }
            }
        }
    }
}