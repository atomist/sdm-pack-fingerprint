query GetFPBySha($sha: String!, $name: String!) {
  AtomistFingerprint(commit: $sha, name: $name) {
    id
    sha
    data
    push {
      branch
      commits {
        repo {
          name
        }
        sha
      }
    }
  }
}
