query PolicyTargetScopes($name: [String]) {
    PolicyTargetScope(name: $name) {
        name
        fingerprints {
            type
            name
        }
        repos {
            includes {
                owner
                name
                branch
            }
            excludes {
                owner
                name
                branch
            }
        }
    }
}
