query FindLinkedReposWithFingerprint {
    Repo {
        name
        owner
        channels {
            name
            id
        }
        branches(name: "master") @required {
            name
            commit {
                pushes @required {
                    fingerprints @required {
                        data
                        name
                    }
                }
            }            
        }
    }
}

